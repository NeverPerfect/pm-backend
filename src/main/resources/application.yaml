spring:
  # Database
  datasource:
    url: jdbc:mysql://172.18.144.1:3306/pm_db
    username: pm_user
    password: iY7rd4xDhoN
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

server:
  port: 8080

# JWT Authentication
jwt:
  secret: MeinSuperGeheimesJwtSecretDasMindestens256BitLangSeinMuss123456
  expiration: 86400000  # 24 hours in milliseconds